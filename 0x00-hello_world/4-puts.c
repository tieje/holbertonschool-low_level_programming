/**
 * main - it print stuff
 * Return: 0
 */
#include <stdio.h>
#include <string.h>
int main(void)
{
char str[51];
strcpy(str, "\"Programming is like building a multilingual puzzle");
puts(str);
return (0);
}
