#include <stdlib.h>
/**
 * _strdup - return a point to newly allocated space
 * @str: some string
 * Return: return a pointer
 */
char *_strdup(char *str)
{
	if (str == NULL)
	{
		return (NULL);
	}
}
