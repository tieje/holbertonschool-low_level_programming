#include <stdlib.h>
/**
 * strtow - splits a string into words
 * @str: string
 * Return: character double pointer
 */
char **strtow(char *str)
{
	if (str == NULL || str == "")
	{
		return (NULL);
	}
	return (NULL);
}
