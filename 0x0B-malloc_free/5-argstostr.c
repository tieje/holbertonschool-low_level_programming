#include <stdlib.h>
/**
 * argstostr - concatenates all args of program
 * @ac: number of args
 * @av: actual args
 * Return: some string
 */
char *argstostr(int ac, char **av)
{
	if (ac == 0 || av == NULL)
	{
		return (NULL);
	}
	return (NULL);
}
